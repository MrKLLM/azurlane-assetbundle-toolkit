# 项目规则

本文件包含碧蓝航线 AssetBundles 解包项目的 agent 行为规则。每次对话自动加载。

## 进度文档维护

每次完成一个任务阶段后，**必须**执行以下步骤：

1. 读取 `PROJECT_STATUS.md` 的当前内容
2. 定位到对应章节（`### 2.X`），更新：
   - 状态emoji：⏳ → ✅（完成）或 ⚠️（部分完成/有问题）
   - 指标表格中的数值（文件数、大小、路径等）
   - `**已知问题**:` 和 `**待解决**:` 部分
3. 更新顶部的 `**生成时间**` 为当前日期
4. 在终端输出确认：`已更新 PROJECT_STATUS.md — [更新的章节]`

如果完成的任务涉及新发现（如新的技术问题、工具需求），同时更新：
- `## 6. 接下来的任务` 中的优先级
- `## 4. 工具链` 如有新工具需求

## 格式规范

PROJECT_STATUS.md 使用以下格式：

- 状态标记：✅ 已完成 / ⚠️ 警告/部分完成 / ⏳ 未处理
- 指标表格：`| 指标 | 数值 |`
- 子章节结构：标题（含emoji）→ 指标表格 → 脚本引用 → 状态说明 → 问题记录 → 待解决事项
- 每个子章节编号：`### 2.X 名称 状态emoji`

## 文件约定

- **PROJECT_STATUS.md** — 进度总结，跨会话对接用
- **DEV_LOG.md** — 操作手册（技术选型、步骤、故障排除），仅在需要更新流程文档时修改
- **ERRORS.log** — 导出/合成错误记录，有新错误时追加

## 脚本约定

- 脚本位于 `scripts/` 目录
- 使用 Python 3.13 + UnityPy 1.25.0
- 运行前先确认依赖已安装
- 大批量操作（>1000 文件）先小规模测试
- **全量运行前必须确认**：修改脚本后，先用 1-3 个样本测试，将结果展示给用户确认正确后，再执行全量运行。未经用户确认不得全量运行。

## Git 规则

- 每次完成任务后自动 commit
- commit message 用中文描述
