# 项目规则

本文件包含碧蓝航线 AssetBundles 解包项目的 agent 行为规则。每次对话自动加载。

## 进度文档维护

`PROJECT_STATUS.md` 是**精简的当前进度快照**，供跨会话快速对接，采用「就地更新」而非「追加流水」。每次完成一个任务阶段后：

1. 读取 `PROJECT_STATUS.md`，定位对应章节（`### 2.X`）
2. **就地更新**该章节：状态 emoji（⏳/⚠️/✅）、指标表格数值、`已知问题 / 待解决`
3. 更新顶部 `**生成时间**` 为当前日期
4. 终端输出确认：`已更新 PROJECT_STATUS.md — [更新的章节]`

**禁止**把逐日会话记录、已解决 bug 的长篇排查、调参流水直接追加进 `PROJECT_STATUS.md`。这类内容必须按下方「文档写入路由」分流到对应文档。

## 格式规范

PROJECT_STATUS.md 各部分进度使用以下格式：

- 状态标记：✅ 已完成 / ⚠️ 警告/部分完成 / ⏳ 未处理
- 指标表格：`| 指标 | 数值 |`
- 子章节结构：标题（含emoji）→ 指标表格 → 脚本引用 → 状态说明 → 问题记录 → 待解决事项
- 每个子章节编号：`### 2.X 名称 状态emoji`

## 文档写入路由（什么内容写进哪个文件）

**每类内容有且只有一个归属文件**，避免重复与膨胀：

| 内容类型 | 归属文件 | 写入方式 |
|---|---|---|
| 当前进度快照、各部分状态、接手须知、待办 | `PROJECT_STATUS.md` | 就地精简更新，**不追加**会话流水 |
| 可复用的操作流程 / 技术选型 / 步骤 | `docs/WORKFLOWS.md` | 按主题追加 |
| 具体问题 → 原因 → 解决方案（踩坑） | `docs/TROUBLESHOOTING.md` | 追加条目 |
| 导出 / 合成运行错误流水 | `docs/ERRORS.log` | 追加 |
| 某个工具的使用说明 | `docs/tools/<工具>使用说明.md` | 就地更新 |
| 一次性审计报告、被取代的旧交接文档 | `docs/archive/` | 移入，文件名带日期 |
| 操作手册（历史主文档） | `docs/DEV_LOG.md` | 仅在流程变更时改 |

## PROJECT_STATUS.md 体量红线

- 只保留：`项目概览 / 各部分状态表 / 当前主线 / 接手须知 / 待办`。
- 逐日会话记录、已解决 bug 的长篇分析、调参流水 → 迁到 `docs/archive/` 或 `docs/TROUBLESHOOTING.md`。
- 超过约 **400 行**即触发归档：当次会话内把溢出内容移入 `docs/archive/`，保持主文档精简。
- 新会话对接只需读 PROJECT_STATUS.md 即可上手；任何“必须翻历史才知道”的内容都属于放错了地方。

## 脚本约定

- 脚本位于 `scripts/` 目录；**可复用的诊断/验证/一次性管线工具放 `scripts/diag/`（必须入库）**。
- `.diag/` 是**临时产物区**（截图、headless Chrome profile、扫描中间件），已写进 `.gitignore`，**不放任何唯一副本的代码**。
- ⚠️ **清理 `.diag/` 或磁盘前必须先核对 `docs/WORKFLOWS.md` WF-15 的「承重文件白名单」**：其中 `.diag/azdata_*.json` 是 `scripts/build_ship_meta.py` 的唯一权威元数据输入，且社区快照会滞后、游戏配置包 `sharecfgdata/*` 为自定义加密无法本机重取——误删不可复原。
- 使用 Python 3.13 + UnityPy 1.25.0
- 运行前先确认依赖已安装
- 大批量操作（>1000 文件）先小规模测试
- **全量运行前必须确认**：修改脚本后，先用 1-3 个样本测试，将结果展示给用户确认正确后，再执行全量运行。未经用户确认不得全量运行。

## Git 规则

- 每次完成任务后自动 commit
- commit message 用中文描述

## 阶段收尾必做清单（DoD —— 没走完不得声称"完成"）

一个任务阶段（修 bug / 加管线 / 改前端 / 做完一轮取证）结束时，**按顺序**走完以下 5 步，缺一不可：

1. **验证跑完，且跑在真实路径上**。按改动面选：
   - 改 `gallery_src/index.html`（Live2D / 前端交互层）→ 必须先 `py -3 scripts/deploy_gallery.py`，再跑 `docs/WORKFLOWS.md` WF-16 回归五件套。**改完不部署等于没改**（真实事故：语音接线代码写完未部署，用户复测仍报"一点声音都没有"，白查一轮）。
   - 改数据管线脚本 → 样本重跑 + 对照组零回退比对，经用户确认后再换入（见 WF-15）。
   - 跑长时批处理（>8 分钟）→ 必须用完全脱离宿主的进程，并三分法判定"真跑完 / 崩了 / 被截断"；不得把 `exit code 0` 当完成证据。
   - 验证脚本不得绕过被测的入口函数（例：验前端交互却直接调库的 `startMotion`，会绕过页面自己的 `play()` → 假阴性）。
2. **文档同步**，按上面「文档写入路由」逐项落，不接受"等以后再补"：
   - 状态/指标变化 → `PROJECT_STATUS.md` 对应 `### 2.X` 就地更新 + 顶部 `生成时间`。
   - 新的可复用流程 → `docs/WORKFLOWS.md` 新增 `WF-n`，必须含**判据**、**踩坑记录**、**涉及文件**三段。
   - 新的「问题 → 根因 → 修法」→ `docs/TROUBLESHOOTING.md` 新增 `§n`。
   - **否证结论同样必须入库**：本轮试过并排除掉的假设（编码方向、密钥形态、格式猜测……）写进对应条目的「已排除的假设」，否则下个会话会原样重跑一遍浪费的时间。
3. **临时探针处置**：本次在 `.diag/` 下新写的 `.py`，要么移进 `scripts/diag/` 入库，要么删掉——`.diag/` 不留任何唯一副本代码。
4. **提交**：`git add` 本次相关文件后 commit（中文 message）。提交前自查 `git status`，确认没把 `.diag/` 产物、`Output/` 运行目录、以及任何疑似密钥/凭据的文件加进去。
5. **输出确认行**（终端可见，供用户核对没跳步）：
   `收尾完成 — 文档: <改了哪几个文件> | 验证: <跑了哪些> | 探针: <入库/删除>`

### 提交闸门（pre-commit hook）

上面第 2 条由 hook 强制：**暂存区改了 `scripts/**` 或 `gallery_src/**`，但没同时暂存任何文档**（`PROJECT_STATUS.md` / `docs/**`）→ 拦下提交。

- 脚本正本（可版本化）：`scripts/git-hooks/pre-commit`
- 生效副本：`.git/hooks/pre-commit`（git 不跟踪该目录，**换机器或重新 clone 后必须重装一次**）：
  `cp scripts/git-hooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit`
- 确属无需改文档的提交（纯重命名、格式化、试验性提交）时绕行：`DOC_CHECK_SKIP=1 git commit ...`（hook 会打印一行警告，不静默放行）。
- 改 hook 逻辑只改 `scripts/git-hooks/pre-commit`，改完记得重新 copy。

## 执行模式约定

- 默认采用「先确认后执行」模式：动手改脚本或跑全量之前，先在对话中给出方案与计划，经用户明确同意后再执行。
- 任何会改动文件、跑批量（>1000 文件）、或可能影响已有正确结果的修改，必须先在对话中展示：目标 / 改动文件 / 步骤 / 风险 / 验证方式，并提供 1-3 个样本测试方案，待用户确认后再执行。
- 计划未经确认，不得擅自全量运行。
